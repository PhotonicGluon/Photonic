{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "pnpm run dev || exit $?",
            "name": "Development Server",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}
